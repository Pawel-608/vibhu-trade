import { OnboardingFlow } from "@/auth/OnboardingFlow";

/** `/onboarding` — invite activation -> trader registration (PLAN.md §7 #2). */
export default function OnboardingPage() {
  return <OnboardingFlow />;
}
